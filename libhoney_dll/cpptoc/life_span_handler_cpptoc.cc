// Copyright (c) 2023 The Honeycomb Authors. All rights
// reserved. Use of this source code is governed by a BSD-style license that
// can be found in the LICENSE file.
//
// ---------------------------------------------------------------------------
//
// This file was generated by the Honeycomb translator tool. If making changes by
// hand only do so within the body of existing method and function
// implementations. See the translator.README.txt file in the tools directory
// for more information.
//
// $hash=e96fe0660b55afa20c3bb5fdebc85635ccc53d09$
//

#include "libhoney_dll/cpptoc/life_span_handler_cpptoc.h"
#include "libhoney_dll/cpptoc/client_cpptoc.h"
#include "libhoney_dll/ctocpp/browser_ctocpp.h"
#include "libhoney_dll/ctocpp/dictionary_value_ctocpp.h"
#include "libhoney_dll/ctocpp/frame_ctocpp.h"
#include "libhoney_dll/shutdown_checker.h"
#include "libhoney_dll/template_util.h"

namespace {

// MEMBER FUNCTIONS - Body may be edited by hand.

int HONEYCOMB_CALLBACK life_span_handler_on_before_popup(
    struct _honey_life_span_handler_t* self,
    honey_browser_t* browser,
    honey_frame_t* frame,
    const honey_string_t* target_url,
    const honey_string_t* target_frame_name,
    honey_window_open_disposition_t target_disposition,
    int user_gesture,
    const honey_popup_features_t* popupFeatures,
    honey_window_info_t* windowInfo,
    honey_client_t** client,
    struct _honey_browser_settings_t* settings,
    struct _honey_dictionary_value_t** extra_info,
    int* no_javascript_access) {
  shutdown_checker::AssertNotShutdown();

  // AUTO-GENERATED CONTENT - DELETE THIS COMMENT BEFORE MODIFYING

  DCHECK(self);
  if (!self) {
    return 0;
  }
  // Verify param: browser; type: refptr_diff
  DCHECK(browser);
  if (!browser) {
    return 0;
  }
  // Verify param: frame; type: refptr_diff
  DCHECK(frame);
  if (!frame) {
    return 0;
  }
  // Verify param: popupFeatures; type: simple_byref_const
  DCHECK(popupFeatures);
  if (!popupFeatures) {
    return 0;
  }
  // Verify param: windowInfo; type: struct_byref
  DCHECK(windowInfo);
  if (!windowInfo) {
    return 0;
  }
  if (!template_util::has_valid_size(windowInfo)) {
    DCHECK(false) << "invalid windowInfo->[base.]size";
    return 0;
  }
  // Verify param: client; type: refptr_same_byref
  DCHECK(client);
  if (!client) {
    return 0;
  }
  // Verify param: settings; type: struct_byref
  DCHECK(settings);
  if (!settings) {
    return 0;
  }
  if (!template_util::has_valid_size(settings)) {
    DCHECK(false) << "invalid settings->[base.]size";
    return 0;
  }
  // Verify param: extra_info; type: refptr_diff_byref
  DCHECK(extra_info);
  if (!extra_info) {
    return 0;
  }
  // Verify param: no_javascript_access; type: bool_byaddr
  DCHECK(no_javascript_access);
  if (!no_javascript_access) {
    return 0;
  }
  // Unverified params: target_url, target_frame_name

  // Translate param: popupFeatures; type: simple_byref_const
  HoneycombPopupFeatures popupFeaturesVal =
      popupFeatures ? *popupFeatures : HoneycombPopupFeatures();
  // Translate param: windowInfo; type: struct_byref
  HoneycombWindowInfo windowInfoObj;
  if (windowInfo) {
    windowInfoObj.AttachTo(*windowInfo);
  }
  // Translate param: client; type: refptr_same_byref
  HoneycombRefPtr<HoneycombClient> clientPtr;
  if (client && *client) {
    clientPtr = HoneycombClientCppToC::Unwrap(*client);
  }
  HoneycombClient* clientOrig = clientPtr.get();
  // Translate param: settings; type: struct_byref
  HoneycombBrowserSettings settingsObj;
  if (settings) {
    settingsObj.AttachTo(*settings);
  }
  // Translate param: extra_info; type: refptr_diff_byref
  HoneycombRefPtr<HoneycombDictionaryValue> extra_infoPtr;
  if (extra_info && *extra_info) {
    extra_infoPtr = HoneycombDictionaryValueCToCpp::Wrap(*extra_info);
  }
  HoneycombDictionaryValue* extra_infoOrig = extra_infoPtr.get();
  // Translate param: no_javascript_access; type: bool_byaddr
  bool no_javascript_accessBool =
      (no_javascript_access && *no_javascript_access) ? true : false;

  // Execute
  bool _retval = HoneycombLifeSpanHandlerCppToC::Get(self)->OnBeforePopup(
      HoneycombBrowserCToCpp::Wrap(browser), HoneycombFrameCToCpp::Wrap(frame),
      HoneycombString(target_url), HoneycombString(target_frame_name), target_disposition,
      user_gesture ? true : false, popupFeaturesVal, windowInfoObj, clientPtr,
      settingsObj, extra_infoPtr, &no_javascript_accessBool);

  // Restore param: windowInfo; type: struct_byref
  if (windowInfo) {
    windowInfoObj.DetachTo(*windowInfo);
  }
  // Restore param: client; type: refptr_same_byref
  if (client) {
    if (clientPtr.get()) {
      if (clientPtr.get() != clientOrig) {
        *client = HoneycombClientCppToC::Wrap(clientPtr);
      }
    } else {
      *client = nullptr;
    }
  }
  // Restore param: settings; type: struct_byref
  if (settings) {
    settingsObj.DetachTo(*settings);
  }
  // Restore param: extra_info; type: refptr_diff_byref
  if (extra_info) {
    if (extra_infoPtr.get()) {
      if (extra_infoPtr.get() != extra_infoOrig) {
        *extra_info = HoneycombDictionaryValueCToCpp::Unwrap(extra_infoPtr);
      }
    } else {
      *extra_info = nullptr;
    }
  }
  // Restore param: no_javascript_access; type: bool_byaddr
  if (no_javascript_access) {
    *no_javascript_access = no_javascript_accessBool ? true : false;
  }

  // Return type: bool
  return _retval;
}

void HONEYCOMB_CALLBACK
life_span_handler_on_after_created(struct _honey_life_span_handler_t* self,
                                   honey_browser_t* browser) {
  shutdown_checker::AssertNotShutdown();

  // AUTO-GENERATED CONTENT - DELETE THIS COMMENT BEFORE MODIFYING

  DCHECK(self);
  if (!self) {
    return;
  }
  // Verify param: browser; type: refptr_diff
  DCHECK(browser);
  if (!browser) {
    return;
  }

  // Execute
  HoneycombLifeSpanHandlerCppToC::Get(self)->OnAfterCreated(
      HoneycombBrowserCToCpp::Wrap(browser));
}

int HONEYCOMB_CALLBACK
life_span_handler_do_close(struct _honey_life_span_handler_t* self,
                           honey_browser_t* browser) {
  shutdown_checker::AssertNotShutdown();

  // AUTO-GENERATED CONTENT - DELETE THIS COMMENT BEFORE MODIFYING

  DCHECK(self);
  if (!self) {
    return 0;
  }
  // Verify param: browser; type: refptr_diff
  DCHECK(browser);
  if (!browser) {
    return 0;
  }

  // Execute
  bool _retval = HoneycombLifeSpanHandlerCppToC::Get(self)->DoClose(
      HoneycombBrowserCToCpp::Wrap(browser));

  // Return type: bool
  return _retval;
}

void HONEYCOMB_CALLBACK
life_span_handler_on_before_close(struct _honey_life_span_handler_t* self,
                                  honey_browser_t* browser) {
  shutdown_checker::AssertNotShutdown();

  // AUTO-GENERATED CONTENT - DELETE THIS COMMENT BEFORE MODIFYING

  DCHECK(self);
  if (!self) {
    return;
  }
  // Verify param: browser; type: refptr_diff
  DCHECK(browser);
  if (!browser) {
    return;
  }

  // Execute
  HoneycombLifeSpanHandlerCppToC::Get(self)->OnBeforeClose(
      HoneycombBrowserCToCpp::Wrap(browser));
}

}  // namespace

// CONSTRUCTOR - Do not edit by hand.

HoneycombLifeSpanHandlerCppToC::HoneycombLifeSpanHandlerCppToC() {
  GetStruct()->on_before_popup = life_span_handler_on_before_popup;
  GetStruct()->on_after_created = life_span_handler_on_after_created;
  GetStruct()->do_close = life_span_handler_do_close;
  GetStruct()->on_before_close = life_span_handler_on_before_close;
}

// DESTRUCTOR - Do not edit by hand.

HoneycombLifeSpanHandlerCppToC::~HoneycombLifeSpanHandlerCppToC() {
  shutdown_checker::AssertNotShutdown();
}

template <>
HoneycombRefPtr<HoneycombLifeSpanHandler> HoneycombCppToCRefCounted<
    HoneycombLifeSpanHandlerCppToC,
    HoneycombLifeSpanHandler,
    honey_life_span_handler_t>::UnwrapDerived(HoneycombWrapperType type,
                                            honey_life_span_handler_t* s) {
  DCHECK(false) << "Unexpected class type: " << type;
  return nullptr;
}

template <>
HoneycombWrapperType HoneycombCppToCRefCounted<HoneycombLifeSpanHandlerCppToC,
                                   HoneycombLifeSpanHandler,
                                   honey_life_span_handler_t>::kWrapperType =
    WT_LIFE_SPAN_HANDLER;
